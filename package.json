{
  "name": "dsh-verification-workspace",
  "private": true,
  "version": "1.0.0",
  "repository": { "type": "git", "url": "git+https://github.com/bpc-oss/dsh-verification.git" },
  "description": "Bobby verification engine ported to DeepSeek Harness (DSH) Cordis plugins",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.0"
  }
}
